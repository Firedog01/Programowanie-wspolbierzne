<Window x:Class="view.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	    xmlns:view="clr-namespace:view"
        mc:Ignorable="d"
        Title="Współbieżne kulki" Height="450" Width="800" Background="#FF212121">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Canvas Margin="12,12,12,47" Grid.RowSpan="2">
            <Canvas.Background>
                <RadialGradientBrush MappingMode="RelativeToBoundingBox" SpreadMethod="Pad" RadiusX="1" RadiusY="1" Center="0,0">
                    <GradientStop Color="#FF292929" Offset="1"/>
                    <GradientStop Color="#FF404040" Offset="0"/>
                </RadialGradientBrush>
            </Canvas.Background>
            <view:Marble/> 
        </Canvas>
        <Border BorderBrush="#FF404040" BorderThickness="2,2,2,2" Margin="10,12,10,45" Grid.Row="0" Grid.RowSpan="2" CornerRadius="5,5,5,5"/>
        <StackPanel Orientation="Horizontal" Margin="10,85,14,10" Grid.Row="1" MinHeight="25" MaxHeight="25">
            <Button x:Name="startButton" Content="Start" Width="50" Margin="0,0,5,0" Foreground="White" Background="#FF404040" BorderBrush="{x:Null}" Height="25">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="stopButton" Content="Stop" Width="50" Margin="0,0,5,0" Foreground="White" Background="#FF404040" BorderBrush="{x:Null}">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Label Content="Ilość kulek:&#xD;&#xA;" Foreground="White" Height="22" Padding="5,4,5,5" Margin="0,-2,0,0"/>
            <TextBox Text="{Binding Value, ElementName=countSlider, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Background="{x:Null}" Height="20" Width="18" Margin="-2,2,0,0" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" FlowDirection="LeftToRight" Padding="0,0,0,0" BorderBrush="{x:Null}" CaretBrush="White" SelectionBrush="{x:Null}" />
            <Slider x:Name="countSlider" Width="172" Minimum="1" Value="3" SmallChange="1" TickPlacement="BottomRight" Height="25" IsSnapToTickEnabled="True" Foreground="White" RenderTransformOrigin="0.479,0.893" Margin="0,0,0,-10" Maximum="20" Background="Transparent"/>
        </StackPanel>
    </Grid>
</Window>
